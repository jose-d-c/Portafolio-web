---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { proyectosTemporales } from "../../data/content";

export function getStaticPaths() {
  return proyectosTemporales.map((item) => ({
    params: { slug: `programa-${String(item.id).padStart(2, "0")}` },
  }));
}

const { slug } = Astro.params;
---

<BaseLayout title={`Proyecto temporal ${slug}`}>
  <main class="app-shell">
    <section class="panel subpanel">
      <h1>{slug}</h1>
      <p>
        Este es un espacio temporal para mostrar entregas o pruebas rápidas. Puedes reemplazar este contenido con la
        evidencia del ejercicio cuando sea necesario.
      </p>
      <a href="/">← Volver al inicio</a>
    </section>
  </main>
</BaseLayout>
