---
import { programasTemporales } from "./programas";
---

<section class="apartado-temporal">
  <h3 class="subtitulo">Programas temporales (20)</h3>
  <p class="textoSuave descripcion">
    Este bloque es independiente de la seccion de proyectos y vive en
    <code>src/programas-temporales</code> para poder retirarlo sin afectar el resto del portafolio.
  </p>

  <div class="grilla-programas">
    {programasTemporales.map((programa) => (
      <article class="tarjeta-programa panel">
        <h4>{programa.titulo}</h4>
        <p class="textoSuave">{programa.descripcion}</p>
        <a href={`/programas-temporales/${programa.slug}`}>Abrir pagina del programa</a>
      </article>
    ))}
  </div>
</section>

<style>
.apartado-temporal{
  margin-top: 18px;
  padding-top: 16px;
  border-top: 1px dashed var(--borde);
}

.subtitulo{
  margin: 0;
  font-family: var(--fuente-titulos);
  font-size: 24px;
}

.descripcion{
  margin: 8px 0 14px;
}

.grilla-programas{
  display:grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}

.tarjeta-programa{
  border-radius: 16px;
  padding: 14px;
}

.tarjeta-programa h4{
  margin: 0 0 6px;
  font-size: 18px;
}

.tarjeta-programa p{
  margin: 0 0 10px;
  font-size: 16px;
  line-height: 1.5;
}

.tarjeta-programa a{
  display: inline-block;
  font-size: 15px;
  text-decoration: underline;
}

@media (max-width: 760px){
  .grilla-programas{ grid-template-columns: 1fr; }
}
</style>
